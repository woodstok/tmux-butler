#!/usr/bin/env bash

ROOTDIR="$( cd "$(dirname "$0")/.." >/dev/null 2>&1 ; pwd -P )"

# Make quetty filter nonspace tokens
export READER="$ROOTDIR/scripts/capture_panes | $ROOTDIR/scripts/quetty -nospace"
export SELECTOR="fzf"

$ROOTDIR/tmux-butler
